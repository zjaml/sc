//An ID will be auto assigned to a new key,ID is a permanent identifier.
//ID can be read from QR code or the fob for the key.
export const ADD_KEY = "ADD_KEY" //ADD_KEY action is the same to RECEIVE_KEYS action.
